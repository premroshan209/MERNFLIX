# --------------------------------------------------------------------------------
# MERNFLIX — Example environment variables
# Copy this file to `.env` and fill with real secrets. Do NOT commit your .env.
# --------------------------------------------------------------------------------

# Server
SERVER_PORT=8000
NODE_ENV=development

# Client (frontend)
CLIENT_HOST=localhost
CLIENT_PORT=5173
CLIENT_URL=http://localhost:5173
# If your frontend needs to call Razorpay from the client, set:
# REACT_APP_RAZORPAY_KEY_ID=your_razorpay_key_id

# JWT / Auth
JWT_SECRET=your_jwt_secret

# Database (MongoDB) - replace with your connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/your_db_name?retryWrites=true&w=majority

# TMDB (optional) - for movie metadata if used
TMDB_API_KEY=your_tmdb_api_key

# Cloudinary (video uploads/hosting)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Razorpay (payment integration)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Email / SMTP (used by Nodemailer)
# Option A: service name (Gmail, SendGrid, etc.)
EMAIL_SERVICE=Gmail
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_email_password
EMAIL_FROM_NAME="MERNFLIX"
EMAIL_FROM_ADDRESS=no-reply@example.com

# Option B: explicit SMTP settings (use instead of EMAIL_SERVICE if preferred)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password

# Mailtrap (optional - for testing)
MAILTRAP_USERNAME=your_mailtrap_username
MAILTRAP_PASSWORD=your_mailtrap_password
MAILTRAP_INBOX_ID=your_mailtrap_inbox_id
# Example API endpoint (replace placeholder)
# MAILTRAP_ENDPOINT=https://sandbox.api.mailtrap.io/api/send/{your_mailbox_id}

# Misc / Notes
# - After copying to .env, ensure .env is listed in .gitignore.
# - Restart the backend after changing environment variables.
# - Keep secrets safe and rotate keys if they leak.
